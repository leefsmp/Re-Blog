import { connect } from 'react-redux'

import AboutView from '../components/AboutView'

const mapDispatchToProps = {

}

const mapStateToProps = (state) => ({

})

export default connect(
  mapStateToProps,
  mapDispatchToProps)(AboutView)
